# 03-数据工程实战

## 📋 学习目标

阅读完本文档后，你应该能够：
- 掌握从公开渠道获取高质量数据集的方法
- 熟练进行数据清洗、转换和验证
- 运用数据增强技术提升数据集质量
- 为意图识别任务准备完整的训练数据

---

## 1. 数据收集

### 1.1 公开数据集源

#### HuggingFace Datasets

**优势**：数据集丰富、易于加载、格式统一

```python
from datasets import load_dataset

# 加载预训练数据集
dataset = load_dataset("wikimedia/wikipedia", "20231101.zh", split="train")

# 加载对话数据集
sft_dataset = load_dataset("thudm/chatlm3_instruction", split="train")

# 保存为 JSONL
def save_to_jsonl(dataset, output_file, fields=None):
    with open(output_file, 'w', encoding='utf-8') as f:
        for item in dataset:
            if fields:
                item = {k: item[k] for k in fields if k in item}
            f.write(json.dumps(item, ensure_ascii=False) + '\n')

save_to_jsonl(dataset, 'data/wiki_zh.jsonl', fields=['text'])
```

#### ModelScope (阿里魔搭)

**优势**：中文数据集丰富、国内访问快

```python
from modelscope import MsDataset

# 加载数据集
dataset = MsDataset.load('modelscope/chinese-wikipedia', split='train')

# 转换为 JSONL
with open('data/wiki_zh.jsonl', 'w', encoding='utf-8') as f:
    for item in dataset:
        f.write(json.dumps({'text': item['text']}, ensure_ascii=False) + '\n')
```

#### GitHub 数据集仓库

**推荐仓库**：
- **wikipedia-dump**：维基百科完整数据
- **common-crawl**：全网网页数据
- **clue**：中文语言理解测评数据集

```bash
# 从 GitHub 下载
git clone https://github.com/CLUEbenchmark/CLUE.git
cd CLUE/datasets
```

### 1.2 意图识别数据集推荐

#### ATIS (Airline Travel Information System)

**描述**：航空旅行意图识别数据集

**下载**：
```bash
# 方式 1：HuggingFace
dataset = load_dataset("atis", split="train")

# 方式 2：直接下载
wget https://github.com/yvchen/JointSLU/raw/master/data/atis.train.w-intent.iob
```

**数据格式**：
```
i want to fly from boston to denver ->atis_flight
what flights are available from pittsburgh to baltimore ->atis_aircraft
```

**转换为 MiniMind 格式**：
```python
def convert_atis_to_jsonl(input_file, output_file):
    """将 ATIS 数据转换为意图识别格式"""
    with open(input_file, 'r', encoding='utf-8') as f_in, \
         open(output_file, 'w', encoding='utf-8') as f_out:
        for line in f_in:
            parts = line.strip().split('->')
            if len(parts) == 2:
                query, intent = parts
                # 构造 SFT 格式
                data = {
                    "conversations": [
                        {"from": "human", "value": f"识别意图：{query}"},
                        {"from": "gpt", "value": intent}
                    ]
                }
                f_out.write(json.dumps(data, ensure_ascii=False) + '\n')

convert_atis_to_jsonl('data/atis.train.w-intent.iob', 'data/atis_sft.jsonl')
```

#### SNIPS (语音助手意图识别)

**描述**：智能家居语音助手意图识别数据集

**下载**：
```bash
# HuggingFace
dataset = load_dataset("snips_built_in_intents", split="train")
```

**意图类别**：
- `AddToPlaylist`：添加歌曲到播放列表
- `BookRestaurant`：预订餐厅
- `GetWeather`：查询天气
- `PlayMusic`：播放音乐
- `RateBook`：评分书籍

#### 自定义意图识别数据集

**场景示例**：客服对话意图识别

```python
# 定义意图类别
INTENTS = {
    "query_balance": "查询余额",
    "transfer_money": "转账",
    "report_loss": "挂失",
    "change_password": "修改密码",
    "consult_product": "咨询产品",
}

# 生成示例数据
def generate_intent_examples():
    examples = []

    # query_balance 示例
    queries = [
        "我还有多少钱？",
        "查询一下账户余额",
        "帮我看看银行卡里有多少钱",
        "余额查询",
    ]
    for q in queries:
        examples.append({
            "conversations": [
                {"from": "human", "value": f"识别意图：{q}"},
                {"from": "gpt", "value": "query_balance"}
            ]
        })

    # transfer_money 示例
    queries = [
        "我要转账给张三",
        "转500块到这个账户",
        "汇款到工行卡",
    ]
    for q in queries:
        examples.append({
            "conversations": [
                {"from": "human", "value": f"识别意图：{q}"},
                {"from": "gpt", "value": "transfer_money"}
            ]
        })

    return examples

# 保存
examples = generate_intent_examples()
with open('data/intent_sft.jsonl', 'w', encoding='utf-8') as f:
    for ex in examples:
        f.write(json.dumps(ex, ensure_ascii=False) + '\n')
```

### 1.3 数据版权和合规性

**注意事项**：

| 数据源 | 许可证 | 可商用 | 备注 |
|--------|--------|--------|------|
| Wikipedia | CC BY-SA 3.0 | ✅ 需署名 | 遵循相同许可 |
| Common Crawl | CC0 | ✅ 可自由使用 | 网页数据需筛选 |
| OpenAI SFT | 自定义 | ❌ 仅研究 | 不可商用 |
| 自有数据 | 自定义 | ✅ 可商用 | 确保有权限 |

**最佳实践**：
1. 记录数据来源和许可证
2. 对敏感信息进行脱敏
3. 遵守 GDPR 等隐私法规
4. 添加数据集说明文件（Data Card）

---

## 2. 数据清洗

### 2.1 格式统一

#### XML → JSONL

```python
import xml.etree.ElementTree as ET

def xml_to_jsonl(xml_file, jsonl_file, format_type='pretrain'):
    """将 XML 转换为 JSONL"""
    tree = ET.parse(xml_file)
    root = tree.getroot()

    with open(jsonl_file, 'w', encoding='utf-8') as f:
        if format_type == 'pretrain':
            for doc in root.findall('.//doc'):
                text = doc.text
                if text and len(text.strip()) > 10:
                    f.write(json.dumps({"text": text.strip()}, ensure_ascii=False) + '\n')

        elif format_type == 'sft':
            for conv in root.findall('.//conversation'):
                turns = []
                for turn in conv.findall('turn'):
                    role = turn.get('role', 'user')
                    content = turn.text
                    if content:
                        turns.append({"from": role, "value": content})
                if len(turns) >= 2:
                    f.write(json.dumps({"conversations": turns}, ensure_ascii=False) + '\n')

xml_to_jsonl('data/raw.xml', 'data/pretrain_hq.jsonl', format_type='pretrain')
```

#### CSV → JSONL

```python
import pandas as pd

def csv_to_jsonl(csv_file, jsonl_file, text_column='text', label_column=None):
    """CSV 转 JSONL"""
    df = pd.read_csv(csv_file)

    with open(jsonl_file, 'w', encoding='utf-8') as f:
        for _, row in df.iterrows():
            if label_column:
                # 分类任务：带标签
                data = {
                    "text": row[text_column],
                    "label": row[label_column]
                }
            else:
                # 预训练任务：纯文本
                data = {"text": row[text_column]}
            f.write(json.dumps(data, ensure_ascii=False) + '\n')

csv_to_jsonl('data/wikipedia.csv', 'data/wiki.jsonl', text_column='content')
```

### 2.2 去重和质量过滤

```python
import hashlib
from typing import Set

def deduplicate_jsonl(input_file, output_file, field='text'):
    """去重（基于内容哈希）"""
    seen_hashes: Set[str] = set()

    with open(input_file, 'r', encoding='utf-8') as f_in, \
         open(output_file, 'w', encoding='utf-8') as f_out:

        for line_num, line in enumerate(f_in, 1):
            try:
                data = json.loads(line.strip())
                content = data.get(field, '')

                # 计算哈希
                content_hash = hashlib.md5(content.encode()).hexdigest()

                if content_hash not in seen_hashes:
                    seen_hashes.add(content_hash)
                    f_out.write(line)

            except Exception as e:
                print(f"Line {line_num}: {e}")

    print(f"去重前: {line_num} 行")
    print(f"去重后: {len(seen_hashes)} 行")
    print(f"去重率: {(1 - len(seen_hashes) / line_num) * 100:.2f}%")

deduplicate_jsonl('data/raw.jsonl', 'data/dedup.jsonl')
```

**质量过滤**：

```python
def quality_filter(input_file, output_file, min_length=20, max_length=10000):
    """质量过滤"""
    from transformers import AutoTokenizer
    tokenizer = AutoTokenizer.from_pretrained('../model/')

    with open(input_file, 'r', encoding='utf-8') as f_in, \
         open(output_file, 'w', encoding='utf-8') as f_out:

        kept = 0
        total = 0

        for line in f_in:
            total += 1
            data = json.loads(line.strip())
            text = data.get('text', '')

            # 过滤条件
            tokens = tokenizer.encode(text)
            token_count = len(tokens)

            # 长度过滤
            if token_count < min_length or token_count > max_length:
                continue

            # 特殊字符比例过滤
            special_ratio = sum(not c.isalnum() and not c.isspace() for c in text) / len(text)
            if special_ratio > 0.3:
                continue

            # 中文字符比例（针对中文数据）
            chinese_ratio = sum('\u4e00' <= c <= '\u9fff' for c in text) / len(text)
            if chinese_ratio < 0.3:  # 至少30%中文
                continue

            # 通过所有过滤
            f_out.write(json.dumps(data, ensure_ascii=False) + '\n')
            kept += 1

    print(f"保留率: {kept / total * 100:.2f}% ({kept}/{total})")

quality_filter('data/dedup.jsonl', 'data/pretrain_hq.jsonl')
```

### 2.3 长度控制

**截断策略**：

```python
def truncate_text(text, max_length=512, tokenizer):
    """智能截断（保留完整句子）"""
    tokens = tokenizer.encode(text)

    if len(tokens) <= max_length:
        return text

    # 尝试在句子边界截断
    truncated = tokens[:max_length]

    # 找到最后一个句号/问号/感叹号
    for i in range(max_length - 1, 0, -1):
        token = truncated[i]
        decoded = tokenizer.decode([token])
        if decoded in ['。', '？', '！', '.', '?', '!']:
            truncated = truncated[:i+1]
            break

    return tokenizer.decode(truncated)

# 使用
from transformers import AutoTokenizer
tokenizer = AutoTokenizer.from_pretrained('../model/')

long_text = "这是一段很长的文本..." * 100
short_text = truncate_text(long_text, max_length=512, tokenizer=tokenizer)
```

**过滤过长样本**：

```python
def filter_long_samples(input_file, output_file, max_length=512, tokenizer):
    """过滤过长的样本"""
    with open(input_file, 'r', encoding='utf-8') as f_in, \
         open(output_file, 'w', encoding='utf-8') as f_out:

        for line in f_in:
            data = json.loads(line.strip())
            text = data.get('text', '')
            tokens = tokenizer.encode(text)

            if len(tokens) <= max_length:
                f_out.write(line)
```

### 2.4 特殊字符和编码问题处理

```python
import re

def clean_special_chars(text):
    """清理特殊字符"""
    # 统一引号
    text = text.replace('"', '"').replace('"', '"')
    text = text.replace(''', "'").replace(''', "'")

    # 去除控制字符
    text = re.sub(r'[\x00-\x08\x0b-\x0c\x0e-\x1f\x7f-\x9f]', '', text)

    # 统一空格
    text = re.sub(r'\s+', ' ', text)

    # 去除 HTML 标签
    text = re.sub(r'<[^>]+>', '', text)

    return text.strip()

def fix_encoding_issues(text):
    """修复编码问题"""
    # 常见编码错误修复
    fixes = {
        'ï¼Œ': '，',
        'ï¼š': '：',
        'ï¼ˆ': '（',
        'ï¼‰': '）',
        'â€™': "'",
        'â€œ': '"',
        'â€\x9d': '"',
    }

    for wrong, correct in fixes.items():
        text = text.replace(wrong, correct)

    return text

# 综合清洗
def clean_text_pipeline(text):
    text = fix_encoding_issues(text)
    text = clean_special_chars(text)
    return text
```

---

## 3. 数据增强

### 3.1 使用大模型生成相似样本

```python
import requests

def generate_similar_examples(original_examples, api_key, num_variations=3):
    """使用大模型生成变体"""
    variations = []

    for example in original_examples:
        original_query = example['conversations'][0]['value']
        intent = example['conversations'][1]['value']

        prompt = f"""生成 {num_variations} 个表达相同意图但措辞不同的查询。
意图类别：{intent}
原始查询：{original_query}

请只输出查询，每行一个："""

        # 调用 API（以 OpenAI 为例）
        response = requests.post(
            'https://api.openai.com/v1/chat/completions',
            headers={'Authorization': f'Bearer {api_key}'},
            json={
                'model': 'gpt-3.5-turbo',
                'messages': [{'role': 'user', 'content': prompt}],
                'temperature': 0.8
            }
        )

        if response.status_code == 200:
            generated_queries = response.json()['choices'][0]['message']['content'].strip().split('\n')

            for query in generated_queries:
                if query.strip():
                    variations.append({
                        "conversations": [
                            {"from": "human", "value": f"识别意图：{query.strip()}"},
                            {"from": "gpt", "value": intent}
                        ]
                    })

    return variations

# 使用
original_examples = [
    {"conversations": [
        {"from": "human", "value": "识别意图：我还有多少钱？"},
        {"from": "gpt", "value": "query_balance"}
    ]}
]

augmented = generate_similar_examples(original_examples, api_key="your-key")
```

### 3.2 回译法（Back Translation）

```python
from transformers import pipeline

# 使用翻译模型
translator_en_zh = pipeline("translation", model="Helsinki-NLP/opus-mt-en-zh")
translator_zh_en = pipeline("translation", model="Helsinki-NLP/opus-mt-zh-en")

def back_translate(text, num_cycles=1):
    """回译增强"""
    current = text

    for _ in range(num_cycles):
        # 中文 → 英文
        en = translator_en_zh(current)[0]['translation_text']
        # 英文 → 中文
        current = translator_zh_en(en)[0]['translation_text']

    return current

# 使用
original = "我要查询银行卡余额"
augmented = back_translate(original, num_cycles=1)
print(f"原始: {original}")
print(f"增强: {augmented}")
```

### 3.3 同义词替换和句式变换

```python
import random

# 同义词词典（示例）
SYNONYMS = {
    "查询": ["查看", "查看一下", "帮我看看", "我想知道"],
    "余额": ["剩下的钱", "账户金额", "存款"],
    "转账": ["汇款", "打钱", "转钱"],
    "多少钱": ["多少金额", "多少钱款", "资金"],
}

def synonym_replace(text, n=1):
    """同义词替换"""
    words = text.split()
    new_words = words.copy()

    # 随机选择 n 个词进行替换
    candidates = [w for w in words if w in SYNONYMS]
    random.shuffle(candidates)

    for word in candidates[:n]:
        synonym = random.choice(SYNONYMS[word])
        new_words = [synonym if w == word else w for w in new_words]

    return ' '.join(new_words)

# 使用
original = "我要查询账户余额"
augmented = synonym_replace(original, n=2)
print(augmented)  # "我想看看账户金额"
```

**句式变换模板**：

```python
TEMPLATES = {
    "query_balance": [
        "我要{查询}{余额}",
        "帮我{查询}一下{余额}",
        "我想知道{余额}是多少",
        "看看{余额}",
        "{余额}{查询}",
    ]
}

def template_based_generation(intent, values):
    """基于模板生成"""
    templates = TEMPLATES.get(intent, [])

    variations = []
    for template in templates:
        try:
            text = template.format(**values)
            variations.append(text)
        except KeyError:
            continue

    return variations

# 使用
variations = template_based_generation(
    "query_balance",
    {"查询": "查询", "余额": "银行卡余额"}
)

for v in variations:
    print(v)
```

### 3.4 意图保持的数据改写技巧

```python
def intent_preserving_rewrite(query, intent):
    """意图保持的改写"""
    # 提取关键信息（需要 NER）
    # 这里简化处理

    # 改写策略
    strategies = [
        lambda q: f"请{q}" if not q.startswith('请') else q,
        lambda q: f"我想{q}" if not q.startswith('我') else q,
        lambda q: f"帮{q}" if not q.startswith('帮') else q,
    ]

    variations = [query]
    for strategy in strategies:
        try:
            new_q = strategy(query)
            if new_q != query:
                variations.append(new_q)
        except:
            pass

    return list(set(variations))
```

### 3.5 类别平衡策略

```python
from collections import Counter

def analyze_class_balance(jsonl_file):
    """分析类别分布"""
    intents = []

    with open(jsonl_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            # 假设格式是 "识别意图：查询" -> "query"
            if 'conversations' in data:
                answer = data['conversations'][1]['value']
                intents.append(answer)

    counter = Counter(intents)

    print("类别分布：")
    for intent, count in counter.most_common():
        print(f"  {intent}: {count}")

    return counter

def balance_dataset(input_file, output_file, max_samples_per_class=1000):
    """过采样平衡数据集"""
    counter = analyze_class_balance(input_file)

    # 收集各类别样本
    class_samples = {}
    with open(input_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            intent = data['conversations'][1]['value']

            if intent not in class_samples:
                class_samples[intent] = []
            class_samples[intent].append(data)

    # 过采样到目标数量
    with open(output_file, 'w', encoding='utf-8') as f:
        for intent, samples in class_samples.items():
            current_count = len(samples)
            target_count = min(max_samples_per_class, current_count)

            # 如果样本不足，重复采样
            while len(samples) < target_count:
                samples.extend(samples[:target_count - len(samples)])

            for sample in samples[:target_count]:
                f.write(json.dumps(sample, ensure_ascii=False) + '\n')

balance_dataset('data/intent_raw.jsonl', 'data/intent_balanced.jsonl')
```

---

## 4. 数据质量评估

### 4.1 数据分布可视化

```python
import matplotlib.pyplot as plt
import numpy as np
from transformers import AutoTokenizer

def visualize_length_distribution(jsonl_file, tokenizer_path='../model/'):
    """可视化长度分布"""
    tokenizer = AutoTokenizer.from_pretrained(tokenizer_path)

    lengths = []
    with open(jsonl_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            if 'text' in data:
                tokens = tokenizer.encode(data['text'])
            elif 'conversations' in data:
                # 对话数据
                prompt = tokenizer.apply_chat_template(
                    [{"role": 'user' if i % 2 == 0 else 'assistant',
                      "content": t['value']}
                     for i, t in enumerate(data['conversations'])],
                    tokenize=False
                )
                tokens = tokenizer.encode(prompt)
            lengths.append(len(tokens))

    # 绘图
    fig, axes = plt.subplots(1, 2, figsize=(15, 5))

    # 直方图
    axes[0].hist(lengths, bins=50, edgecolor='black', alpha=0.7)
    axes[0].set_xlabel('Token Count')
    axes[0].set_ylabel('Frequency')
    axes[0].set_title('Length Distribution')

    # 箱线图
    axes[1].boxplot(lengths, vert=False)
    axes[1].set_xlabel('Token Count')
    axes[1].set_title('Length Box Plot')

    # 统计信息
    stats = {
        'Mean': np.mean(lengths),
        'Median': np.median(lengths),
        'Std': np.std(lengths),
        'Min': np.min(lengths),
        'Max': np.max(lengths),
        '90%': np.percentile(lengths, 90),
        '95%': np.percentile(lengths, 95),
        '99%': np.percentile(lengths, 99),
    }

    print("长度统计：")
    for k, v in stats.items():
        print(f"  {k}: {v:.2f}")

    plt.tight_layout()
    plt.savefig('docs/data_length_distribution.png', dpi=300)
    print("分布图已保存")

visualize_length_distribution('data/pretrain_hq.jsonl')
```

### 4.2 标签一致性检查

```python
def check_label_consistency(jsonl_file):
    """检查标签一致性"""
    from difflib import SequenceMatcher

    # 收集相似样本
    samples = []
    with open(jsonl_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            query = data['conversations'][0]['value']
            intent = data['conversations'][1]['value']
            samples.append((query, intent))

    # 检查相似查询的标签是否一致
    inconsistencies = []
    for i, (query1, intent1) in enumerate(samples):
        for query2, intent2 in samples[i+1:]:
            # 计算相似度
            similarity = SequenceMatcher(None, query1, query2).ratio()

            # 如果相似但标签不同
            if similarity > 0.8 and intent1 != intent2:
                inconsistencies.append({
                    'query1': query1,
                    'intent1': intent1,
                    'query2': query2,
                    'intent2': intent2,
                    'similarity': similarity
                })

    if inconsistencies:
        print(f"发现 {len(inconsistencies)} 组不一致的标注：")
        for inc in inconsistencies[:10]:
            print(f"\n相似度: {inc['similarity']:.2f}")
            print(f"  查询1: {inc['query1']} -> {inc['intent1']}")
            print(f"  查询2: {inc['query2']} -> {inc['intent2']}")
    else:
        print("✅ 标签一致性检查通过")

check_label_consistency('data/intent_sft.jsonl')
```

### 4.3 训练/验证/测试集划分

```python
import random
from sklearn.model_selection import train_test_split

def split_dataset(input_file, train_ratio=0.8, val_ratio=0.1, test_ratio=0.1, seed=42):
    """划分数据集"""
    random.seed(seed)

    # 读取所有数据
    samples = []
    with open(input_file, 'r', encoding='utf-8') as f:
        for line in f:
            samples.append(line.strip())

    # 划分
    train, temp = train_test_split(samples, train_size=train_ratio, random_state=seed)
    val, test = train_test_split(temp, train_size=val_ratio/(val_ratio+test_ratio), random_state=seed)

    # 保存
    def save_split(data, filename):
        with open(filename, 'w', encoding='utf-8') as f:
            for line in data:
                f.write(line + '\n')

    save_split(train, 'data/train.jsonl')
    save_split(val, 'data/val.jsonl')
    save_split(test, 'data/test.jsonl')

    print(f"训练集: {len(train)} 样本 ({len(train)/len(samples)*100:.1f}%)")
    print(f"验证集: {len(val)} 样本 ({len(val)/len(samples)*100:.1f}%)")
    print(f"测试集: {len(test)} 样本 ({len(test)/len(samples)*100:.1f}%)")

split_dataset('data/intent_balanced.jsonl')
```

**分层划分（保持类别分布）**：

```python
def stratified_split(input_file, train_ratio=0.8, val_ratio=0.1, test_ratio=0.1, seed=42):
    """分层划分（保持类别比例）"""
    # 按类别分组
    class_samples = {}
    with open(input_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            intent = data['conversations'][1]['value']

            if intent not in class_samples:
                class_samples[intent] = []
            class_samples[intent].append(line)

    # 对每个类别分别划分
    train_data, val_data, test_data = [], [], []

    for intent, samples in class_samples.items():
        train, temp = train_test_split(samples, train_size=train_ratio, random_state=seed)
        val, test = train_test_split(temp, train_size=val_ratio/(val_ratio+test_ratio), random_state=seed)

        train_data.extend(train)
        val_data.extend(val)
        test_data.extend(test)

    # 保存（使用上面的 save_split 函数）
    def save_split(data, filename):
        with open(filename, 'w', encoding='utf-8') as f:
            for line in data:
                f.write(line if line.endswith('\n') else line + '\n')

    save_split(train_data, 'data/train_stratified.jsonl')
    save_split(val_data, 'data/val_stratified.jsonl')
    save_split(test_data, 'data/test_stratified.jsonl')

stratified_split('data/intent_balanced.jsonl')
```

### 4.4 数据泄漏检测

```python
def detect_data_leakage(train_file, test_file, similarity_threshold=0.9):
    """检测数据泄漏（训练集和测试集过于相似）"""
    from difflib import SequenceMatcher

    # 加载数据
    train_samples = []
    with open(train_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            if 'text' in data:
                train_samples.append(data['text'])
            elif 'conversations' in data:
                train_samples.append(data['conversations'][0]['value'])

    test_samples = []
    with open(test_file, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            if 'text' in data:
                test_samples.append(data['text'])
            elif 'conversations' in data:
                test_samples.append(data['conversations'][0]['value'])

    # 检测泄漏
    leaks = []
    for i, train_text in enumerate(train_samples):
        for j, test_text in enumerate(test_samples):
            similarity = SequenceMatcher(None, train_text, test_text).ratio()
            if similarity >= similarity_threshold:
                leaks.append({
                    'train_idx': i,
                    'test_idx': j,
                    'similarity': similarity,
                    'train_text': train_text[:100],
                    'test_text': test_text[:100]
                })

    if leaks:
        print(f"⚠️  检测到 {len(leaks)} 组可能的泄漏数据：")
        for leak in leaks[:5]:
            print(f"\n相似度: {leak['similarity']:.2f}")
            print(f"  训练集[{leak['train_idx']}]: {leak['train_text']}...")
            print(f"  测试集[{leak['test_idx']}]: {leak['test_text']}...")
    else:
        print("✅ 未检测到数据泄漏")

detect_data_leakage('data/train.jsonl', 'data/test.jsonl')
```

---

## 5. 实战案例：意图识别数据集准备

### 5.1 完整流程

```python
import json
from transformers import AutoTokenizer
from pathlib import Path

def prepare_intent_dataset():
    """准备意图识别数据集的完整流程"""

    # 1. 定义意图类别和示例
    intent_data = {
        "query_balance": [
            "我还有多少钱？",
            "查询一下账户余额",
            "帮我看看银行卡里有多少钱",
            "余额查询",
            "我想知道账户金额",
        ],
        "transfer_money": [
            "我要转账给张三",
            "转500块到这个账户",
            "汇款到工行卡",
            "打钱给朋友",
            "账户转账",
        ],
        "report_loss": [
            "我的卡丢了要挂失",
            "银行卡挂失",
            "我要挂失",
            "卡片丢失了怎么办",
            "申请挂失",
        ],
        "change_password": [
            "修改登录密码",
            "我要改密码",
            "重置密码",
            "忘记密码了",
            "设置新密码",
        ],
    }

    # 2. 生成基础数据
    samples = []
    for intent, queries in intent_data.items():
        for query in queries:
            samples.append({
                "conversations": [
                    {"from": "human", "value": f"识别意图：{query}"},
                    {"from": "gpt", "value": intent}
                ]
            })

    # 3. 保存原始数据
    Path('data').mkdir(exist_ok=True)
    with open('data/intent_raw.jsonl', 'w', encoding='utf-8') as f:
        for sample in samples:
            f.write(json.dumps(sample, ensure_ascii=False) + '\n')

    print(f"✅ 原始数据生成完成：{len(samples)} 条")

    # 4. 数据增强（回译）
    print("🔄 开始数据增强...")
    augmented_samples = []

    for sample in samples:
        original_query = sample['conversations'][0]['value']
        intent = sample['conversations'][1]['value']

        # 提取纯查询（去掉"识别意图："前缀）
        if original_query.startswith('识别意图：'):
            query = original_query.replace('识别意图：', '')
        else:
            query = original_query

        # 简单的改写（实际可使用回译或大模型）
        variations = generate_query_variations(query)

        for var_query in variations:
            augmented_samples.append({
                "conversations": [
                    {"from": "human", "value": f"识别意图：{var_query}"},
                    {"from": "gpt", "value": intent}
                ]
            })

    # 合并原始数据和增强数据
    all_samples = samples + augmented_samples

    # 5. 数据验证
    print("🔍 验证数据...")
    tokenizer = AutoTokenizer.from_pretrained('../model/')

    valid_samples = []
    for sample in all_samples:
        query = sample['conversations'][0]['value']
        intent = sample['conversations'][1]['value']

        # 检查 token 长度
        tokens = tokenizer.encode(query)
        if len(tokens) <= 128:  # 过滤过长的
            valid_samples.append(sample)

    print(f"✅ 验证通过：{len(valid_samples)}/{len(all_samples)} 条")

    # 6. 划分数据集
    print("📊 划分数据集...")
    from sklearn.model_selection import train_test_split

    train, temp = train_test_split(valid_samples, train_size=0.8, random_state=42)
    val, test = train_test_split(temp, train_size=0.5, random_state=42)

    # 保存
    def save_jsonl(data, path):
        with open(path, 'w', encoding='utf-8') as f:
            for sample in data:
                f.write(json.dumps(sample, ensure_ascii=False) + '\n')

    save_jsonl(train, 'data/intent_train.jsonl')
    save_jsonl(val, 'data/intent_val.jsonl')
    save_jsonl(test, 'data/intent_test.jsonl')

    print(f"✅ 数据集划分完成：")
    print(f"  训练集: {len(train)} 条")
    print(f"  验证集: {len(val)} 条")
    print(f"  测试集: {len(test)} 条")

    # 7. 生成报告
    generate_data_report(train, val, test)

def generate_query_variations(query, num_variations=3):
    """生成查询变体（简化版）"""
    variations = [query]  # 包含原始查询

    # 这里可以使用更复杂的增强方法
    # 示例：简单的词序变换
    if '要' in query:
        variations.append(query.replace('要', '想要'))
    if '帮' in query:
        variations.append(query.replace('帮', '帮我'))
    if '一下' not in query and len(query) < 20:
        variations.append(query + '一下')

    return variations[:num_variations]

def generate_data_report(train, val, test):
    """生成数据报告"""
    # 统计类别分布
    from collections import Counter

    def count_intents(data):
        intents = [sample['conversations'][1]['value'] for sample in data]
        return Counter(intents)

    train_dist = count_intents(train)
    val_dist = count_intents(val)
    test_dist = count_intents(test)

    report = f"""
# 意图识别数据集报告

## 数据量统计
- 训练集: {len(train)} 条
- 验证集: {len(val)} 条
- 测试集: {len(test)} 条
- 总计: {len(train) + len(val) + len(test)} 条

## 类别分布

### 训练集
{format_distribution(train_dist)}

### 验证集
{format_distribution(val_dist)}

### 测试集
{format_distribution(test_dist)}

## 意图类别
{list(train_dist.keys())}
"""

    with open('docs/intent_dataset_report.md', 'w', encoding='utf-8') as f:
        f.write(report)

    print("📄 数据报告已生成：docs/intent_dataset_report.md")

def format_distribution(counter):
    """格式化分布"""
    lines = []
    total = sum(counter.values())
    for intent, count in counter.most_common():
        pct = count / total * 100
        lines.append(f"- {intent}: {count} ({pct:.1f}%)")
    return '\n'.join(lines)

# 运行
prepare_intent_dataset()
```

### 5.2 数据质量分析报告

```python
def analyze_intent_dataset():
    """分析意图识别数据集质量"""
    import matplotlib.pyplot as plt
    from transformers import AutoTokenizer

    tokenizer = AutoTokenizer.from_pretrained('../model/')

    # 读取数据
    datasets = {
        'train': 'data/intent_train.jsonl',
        'val': 'data/intent_val.jsonl',
        'test': 'data/intent_test.jsonl',
    }

    analysis = {}

    for split, path in datasets.items():
        with open(path, 'r', encoding='utf-8') as f:
            data = [json.loads(line.strip()) for line in f]

        # 统计
        intents = [sample['conversations'][1]['value'] for sample in data]
        lengths = []

        for sample in data:
            query = sample['conversations'][0]['value']
            tokens = tokenizer.encode(query)
            lengths.append(len(tokens))

        from collections import Counter
        intent_counter = Counter(intents)

        analysis[split] = {
            'count': len(data),
            'intents': intent_counter,
            'avg_length': sum(lengths) / len(lengths),
            'max_length': max(lengths),
            'min_length': min(lengths),
        }

    # 打印报告
    print("=" * 50)
    print("意图识别数据集质量分析报告")
    print("=" * 50)

    for split, stats in analysis.items():
        print(f"\n【{split.upper()}】")
        print(f"样本数: {stats['count']}")
        print(f"平均长度: {stats['avg_length']:.2f} tokens")
        print(f"长度范围: {stats['min_length']} - {stats['max_length']} tokens")
        print(f"\n类别分布:")
        for intent, count in stats['intents'].most_common():
            pct = count / stats['count'] * 100
            print(f"  {intent}: {count} ({pct:.1f}%)")

analyze_intent_dataset()
```

---

## 6. 数据工程最佳实践

### 6.1 版本管理（DVC、Git LFS）

**使用 DVC 管理数据**：

```bash
# 初始化 DVC
dvc init

# 添加数据文件
dvc add data/intent_train.jsonl
dvc add data/intent_val.jsonl
dvc add data/intent_test.jsonl

# 提交到 Git
git add data/intent_train.jsonl.dvc .gitignore
git commit -m "Add intent dataset"

# 推送数据到远程存储
dvc remote add -d myremote s3://my-bucket/dvc-storage
dvc push
```

### 6.2 数据文档（Data Cards）

**创建 Data Card 模板**：

```markdown
# 数据集 Data Card

## 基本信息
- **数据集名称**: 意图识别数据集
- **版本**: v1.0
- **创建日期**: 2026-02-17
- **维护者**: Your Name

## 数据来源
- **来源方式**: 自有数据 + 数据增强
- **收集时间**: 2026年2月
- **语言**: 中文

## 数据统计
- **总样本数**: 1000
- **类别数**: 4
- **平均长度**: 15 tokens

## 类别分布
- query_balance: 300 (30%)
- transfer_money: 250 (25%)
- report_loss: 250 (25%)
- change_password: 200 (20%)

## 数据划分
- 训练集: 800 (80%)
- 验证集: 100 (10%)
- 测试集: 100 (10%)

## 数据质量
- 去重: 是
- 质量过滤: 是
- 标签验证: 是
- 数据泄漏检测: 通过

## 使用限制
- 仅用于研究目的
- 不可用于商业应用
- 引用时请注明来源

## 更新日志
- v1.0 (2026-02-17): 初始版本
```

### 6.3 持续更新策略

```python
def dataset_versioning():
    """数据集版本管理"""
    import shutil
    from datetime import datetime

    # 创建版本目录
    version = datetime.now().strftime("%Y%m%d")
    version_dir = f"data/v{version}"
    Path(version_dir).mkdir(exist_ok=True)

    # 复制当前数据到版本目录
    for file in ['intent_train.jsonl', 'intent_val.jsonl', 'intent_test.jsonl']:
        src = f'data/{file}'
        dst = f'{version_dir}/{file}'
        shutil.copy(src, dst)

    # 更新符号链接（指向最新版本）
    for file in ['intent_train.jsonl', 'intent_val.jsonl', 'intent_test.jsonl']:
        src = f'{version_dir}/{file}'
        dst = f'data/{file}'
        if Path(dst).exists():
            Path(dst).unlink()
        Path(dst).symlink_to(src)

    print(f"✅ 数据集版本 v{version} 已创建")

dataset_versioning()
```

### 6.4 团队协作规范

**数据协作最佳实践**：

1. **统一数据格式**：使用 JSONL 格式
2. **版本控制**：使用 DVC 或 Git LFS
3. **文档记录**：维护 Data Card
4. **代码审查**：数据清洗脚本需审查
5. **自动化测试**：CI/CD 中包含数据验证

---

## 7. 总结

### 数据工程核心步骤

```
收集 → 清洗 → 增强 → 验证 → 划分 → 版本管理
  ↓      ↓      ↓      ↓      ↓        ↓
公开    格式   回译   质量   训练/   DVC
数据    统一   改写   检查   验/测   Git
```

### 关键要点

1. **数据收集**：选择合适的数据源，注意许可证
2. **数据清洗**：去重、过滤、格式统一缺一不可
3. **数据增强**：回译、同义词替换、模板生成
4. **质量评估**：分布分析、一致性检查、泄漏检测
5. **版本管理**：使用 DVC 进行数据版本控制
6. **文档记录**：维护 Data Card 记录数据集信息

---

## 8. 延伸阅读

- **02-数据集处理详解.md**：Dataset 类实现细节
- **07-实战案例-意图识别.md**：完整的意图识别项目实战

---

**文档版本**: v1.0
**最后更新**: 2026-02-17
