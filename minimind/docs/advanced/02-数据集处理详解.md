# 02-数据集处理详解

## 📋 学习目标

阅读完本文档后，你应该能够：
- 理解三种训练阶段（预训练/SFT/DPO）的数据格式差异
- 掌握 Dataset 类的核心实现原理
- 理解动态损失掩码（loss_mask）的作用和实现
- 独立准备自定义数据集并验证其正确性

---

## 1. 数据格式规范

### 1.1 预训练数据格式

预训练的目标是让模型学习语言知识，因此使用**连续文本**作为输入。

**格式**：JSONL（每行一个 JSON 对象）

```jsonl
{"text": "人工智能是计算机科学的一个分支，它企图了解智能的实质，并生产出一种新的能以人类智能相似的方式做出反应的智能机器。"}
{"text": "机器学习是人工智能的核心，是使计算机具有智能的根本途径。"}
{"text": "深度学习是机器学习领域中一个新的研究方向，它被引入机器学习使其更接近于最初的目标——人工智能。"}
```

**关键点**：
- 字段名必须是 `"text"`
- 内容可以是任意长度的中文/英文文本
- 模型会自动截断到 `max_length`

**数据来源示例**：
- 维基百科文章
- 书籍文本
- 网页内容（清洗后）
- 论文摘要

### 1.2 SFT 数据格式（ChatML）

SFT（监督微调）的目标是让模型学习对话能力，使用**多轮对话**格式。

**格式**：JSONL，使用 ChatML 格式

```jsonl
{
  "conversations": [
    {"from": "human", "value": "什么是人工智能？"},
    {"from": "gpt", "value": "人工智能（Artificial Intelligence，简称AI）是计算机科学的一个分支..."}
  ]
}
{
  "conversations": [
    {"from": "human", "value": "帮我写一段 Python 代码"},
    {"from": "gpt", "value": "当然，这是一个简单的示例：\n```python\nprint('Hello, World!')\n```"},
    {"from": "human", "value": "能解释一下吗？"},
    {"from": "gpt", "value": "这段代码很简单，它只是输出 'Hello, World!' 到控制台..."}
  ]
}
```

**ChatML 格式说明**：

对话会被转换为以下模板：
```
<|im_start|>user
什么是人工智能？<|im_end|>
<|im_start|>assistant
人工智能（Artificial Intelligence，简称AI）是计算机科学的一个分支...<|im_end|>
```

**特殊 Token**：
- `<|im_start|>`：BOS (Beginning of Sequence) = 1
- `<|im_end|>`：EOS (End of Sequence) = 2

**关键点**：
- 必须是偶数轮（问-答成对）
- `"from"` 可以是 `"human"`/`"user"` 和 `"gpt"`/`"assistant"`
- `"value"` 是具体的对话内容

### 1.3 DPO 数据格式

DPO（Direct Preference Optimization）的目标是对齐人类偏好，需要**对比数据**。

**格式**：JSONL，包含 chosen 和 rejected 两个对话

```jsonl
{
  "chosen": [
    {"role": "user", "content": "如何学习 Python？"},
    {"role": "assistant", "content": "学习 Python 可以按照以下步骤：1. 基础语法... 2. 实践项目... 3. 深入理解..."}
  ],
  "rejected": [
    {"role": "user", "content": "如何学习 Python？"},
    {"role": "assistant", "content": "我不知道，去问别人。"}
  ]
}
{
  "chosen": [
    {"role": "user", "content": "解释什么是递归"},
    {"role": "assistant", "content": "递归是指函数在执行过程中调用自身...举例说明：计算阶乘..."}
  ],
  "rejected": [
    {"role": "user", "content": "解释什么是递归"},
    {"role": "assistant", "content": "递归就是自己调用自己。"}
  ]
}
```

**关键点**：
- `chosen` 和 `rejected` 必须有相同的 user prompt
- `chosen` 是更好的回答
- `rejected` 是较差的回答
- 模型会学习让 `chosen` 的概率更高

---

## 2. 数据集类实现

### 2.1 PretrainDataset 源码解读

**位置**：`dataset/lm_dataset.py:16-51`

```python
class PretrainDataset(Dataset):
    def __init__(self, data_path, tokenizer, max_length=512):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.samples = self.load_data(data_path)

    def load_data(self, path):
        samples = []
        with open(path, 'r', encoding='utf-8') as f:
            for line_num, line in enumerate(f, 1):
                data = json.loads(line.strip())
                samples.append(data)
        return samples

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, index):
        sample = self.samples[index]

        # 构建输入文本
        encoding = self.tokenizer(
            str(sample['text']),
            max_length=self.max_length,
            padding='max_length',
            truncation=True,
            return_tensors='pt'
        )
        input_ids = encoding.input_ids.squeeze()
        loss_mask = (input_ids != self.tokenizer.pad_token_id)

        X = torch.tensor(input_ids[:-1], dtype=torch.long)
        Y = torch.tensor(input_ids[1:], dtype=torch.long)
        loss_mask = torch.tensor(loss_mask[1:], dtype=torch.long)
        return X, Y, loss_mask
```

**核心逻辑解析**：

1. **数据加载**（`load_data`）：
   - 逐行读取 JSONL 文件
   - 解析 JSON 并存储为列表

2. **Tokenizer 处理**（`__getitem__`）：
   - 将文本转为 token IDs
   - `padding='max_length'`：统一长度
   - `truncation=True`：超长截断

3. **输入输出对构造**：
   ```
   原始: [我, 爱, 编, 程, 序, <PAD>, <PAD>]
   X:    [我, 爱, 编, 程, 序, <PAD>]      # 输入
   Y:    [爱, 编, 程, 序, <PAD>, <PAD>]   # 预测目标（右移一位）
   ```

4. **Loss Mask 作用**：
   ```python
   loss_mask = (input_ids != tokenizer.pad_token_id)
   ```
   - 标记哪些位置需要计算 loss
   - `<PAD>` 位置不参与 loss 计算

**训练时使用**：
```python
# trainer/train_pretrain.py:36-56
for step, (X, Y, loss_mask) in enumerate(train_loader):
    res = model(X)
    loss = loss_fct(res.logits.view(-1, vocab_size), Y.view(-1))
    loss = (loss * loss_mask).sum() / loss_mask.sum()  # 只计算非 pad 位置
```

### 2.2 SFTDataset 源码解读

**位置**：`dataset/lm_dataset.py:54-119`

```python
class SFTDataset(Dataset):
    def __init__(self, jsonl_path, tokenizer, max_length=1024):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.samples = self.load_data(jsonl_path)
        # 获取特殊 token 的 ID
        self.bos_id = tokenizer('<|im_start|>assistant', add_special_tokens=False).input_ids
        self.eos_id = tokenizer('<|im_end|>', add_special_tokens=False).input_ids

    def _create_chat_prompt(self, conversations):
        """构建符合 ChatML 格式的对话"""
        messages = []
        for i, turn in enumerate(conversations):
            role = 'user' if i % 2 == 0 else 'assistant'
            messages.append({"role": role, "content": turn['content']})
        return self.tokenizer.apply_chat_template(
            messages,
            tokenize=False,
            add_generation_prompt=False
        )

    def _generate_loss_mask(self, input_ids):
        """生成动态损失掩码：只在 assistant 回复处计算 loss"""
        loss_mask = [0] * len(input_ids)
        i = 0
        while i < len(input_ids):
            # 查找 <|im_start|>assistant
            if input_ids[i:i + len(self.bos_id)] == self.bos_id:
                start = i + len(self.bos_id)
                end = start
                # 查找对应的 <|im_end|>
                while end < len(input_ids):
                    if input_ids[end:end + len(self.eos_id)] == self.eos_id:
                        break
                    end += 1
                # 标记 assistant 回复部分为 1（参与 loss 计算）
                for j in range(start + 1, min(end + len(self.eos_id) + 1, self.max_length)):
                    loss_mask[j] = 1
                i = end + len(self.eos_id) if end < len(input_ids) else len(input_ids)
            else:
                i += 1
        return loss_mask

    def __getitem__(self, index):
        sample = self.samples[index]
        # 构建对话提示
        prompt = self._create_chat_prompt(sample['conversations'])
        input_ids = self.tokenizer(prompt).input_ids[:self.max_length]
        input_ids += [self.tokenizer.pad_token_id] * (self.max_length - len(input_ids))

        # 生成动态损失掩码
        loss_mask = self._generate_loss_mask(input_ids)

        # 构建训练数据
        X = torch.tensor(input_ids[:-1], dtype=torch.long)
        Y = torch.tensor(input_ids[1:], dtype=torch.long)
        loss_mask = torch.tensor(loss_mask[1:], dtype=torch.long)

        return X, Y, loss_mask
```

**核心逻辑：动态损失掩码**

```
原始对话序列：
<|im_start|>user 你好<|im_end|><|im_start|>assistant 你好！<|im_end|>

Token IDs:
[1, 123, 456, 2, 1, 789, 321, 2]
[1,  <user>,  <user>, <eos>, 1, <asst>, <asst>, <eos>]

Loss Mask (原始):
[0, 0, 0, 0, 0, 1, 1, 1]
       不计算          ↑ 只在 assistant 部分计算

训练目标：模型学习生成 assistant 的回复
```

**为什么需要动态损失掩码？**

1. **User 输入**：是给定的，不需要模型预测 → `loss_mask = 0`
2. **Assistant 回复**：需要模型学习生成 → `loss_mask = 1`

这样训练时，模型只关注学习如何回复，而不是学习模仿用户输入。

### 2.3 DPODataset 源码解读

**位置**：`dataset/lm_dataset.py:122-195`

```python
class DPODataset(Dataset):
    def __init__(self, file_path, tokenizer, max_length=4096):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.padding = tokenizer.pad_token_id if tokenizer.pad_token_id is not None else 0
        self.bos_id = tokenizer('<|im_start|>assistant', add_special_tokens=False).input_ids
        self.eos_id = tokenizer('<|im_end|>', add_special_tokens=False).input_ids
        with open(file_path, 'r', encoding='utf-8') as f:
            self.data = []
            for line in f:
                obj = json.loads(line.strip())
                self.data.append(obj)

    def __getitem__(self, index):
        item = self.data[index]
        chosen = item['chosen']
        rejected = item['rejected']

        # 分别处理 chosen 和 rejected
        chosen_prompt = self.tokenizer.apply_chat_template(chosen, tokenize=False, add_generation_prompt=False)
        rejected_prompt = self.tokenizer.apply_chat_template(rejected, tokenize=False, add_generation_prompt=False)

        chosen_encoding = self.tokenizer(chosen_prompt, truncation=True, max_length=self.max_length, padding='max_length')
        rejected_encoding = self.tokenizer(rejected_prompt, truncation=True, max_length=self.max_length, padding='max_length')

        chosen_input_ids = chosen_encoding['input_ids']
        chosen_loss_mask = self._generate_loss_mask(chosen_input_ids)

        rejected_input_ids = rejected_encoding['input_ids']
        rejected_loss_mask = self._generate_loss_mask(rejected_input_ids)

        # 构建训练数据（返回 chosen 和 rejected 两份）
        x_chosen = torch.tensor(chosen_input_ids[:-1], dtype=torch.long)
        y_chosen = torch.tensor(chosen_input_ids[1:], dtype=torch.long)
        mask_chosen = torch.tensor(chosen_loss_mask[1:], dtype=torch.long)

        x_rejected = torch.tensor(rejected_input_ids[:-1], dtype=torch.long)
        y_rejected = torch.tensor(rejected_input_ids[1:], dtype=torch.long)
        mask_rejected = torch.tensor(rejected_loss_mask[1:], dtype=torch.long)

        return {
            'x_chosen': x_chosen,
            'y_chosen': y_chosen,
            'mask_chosen': mask_chosen,
            'x_rejected': x_rejected,
            'y_rejected': y_rejected,
            'mask_rejected': mask_rejected
        }
```

**DPO 数据返回对比数据**：

训练时，模型会同时处理 chosen 和 rejected 两个样本：
- **chosen**：希望模型输出的概率更高
- **rejected**：希望模型输出的概率更低

**DPO Loss 计算**（简化）：
```python
# 在 trainer/train_dpo.py 中
policy_chosen_logps = compute_log_probs(model, x_chosen, y_chosen, mask_chosen)
policy_rejected_logps = compute_log_probs(model, x_rejected, y_rejected, mask_rejected)

reference_chosen_logps = compute_log_probs(reference_model, x_chosen, y_chosen, mask_chosen)
reference_rejected_logps = compute_log_probs(reference_model, x_rejected, y_rejected, mask_rejected)

# DPO loss
loss = -log_sigmoid(beta * (policy_chosen_logps - policy_rejected_logps
                            - reference_chosen_logps + reference_rejected_logps))
```

---

## 3. Tokenizer 详解

### 3.1 自定义 Tokenizer 的训练

**脚本位置**：`scripts/train_tokenizer.py`

```python
from tokenizers import Tokenizer
from tokenizers.models import BPE
from tokenizers.trainers import BpeTrainer
from tokenizers.pre_tokenizers import Whitespace

# 初始化 BPE tokenizer
tokenizer = Tokenizer(BPE(unk_token="[UNK]"))

# 配置预分词器
tokenizer.pre_tokenizer = Whitespace()

# 训练器
trainer = BpeTrainer(
    vocab_size=6400,  # MiniMind 使用小词表
    special_tokens=["[PAD]", "[BOS]", "[EOS]", "[UNK]"]
)

# 训练
tokenizer.train(files=["data/corpus.txt"], trainer=trainer)

# 保存
tokenizer.save("model/tokenizer.json")
```

**为什么选择 6400 词表？**

| 词表大小 | 优点 | 缺点 | 适用场景 |
|----------|------|------|----------|
| **6400** | 参数少、训练快、适合小模型 | 覆盖率低、OOV 多 | 轻量级模型 |
| **32000** | 覆盖率高、分词合理 | 参数多、训练慢 | 大模型 |
| **100000+** | 几乎无 OOV | 极大、训练成本高 | 超大模型 |

**MiniMind 的选择**：
- 26M 参数模型，6400 词表更合适
- Embedding 层参数：6400 × 512 = 3.3M
- 如果用 32000：32000 × 512 = 16.4M（太大！）

### 3.2 特殊 Token 的作用

```python
MiniMind 配置的特殊 Token：
- pad_token_id = 0   # 填充
- bos_token_id = 1   # <|im_start|>
- eos_token_id = 2   # <|im_end|>
```

**用途**：

1. **PAD (0)**：批处理时填充序列
   ```
   [123, 456, 789, 0, 0, 0]  # 后面的 0 是填充
   ```

2. **BOS (1)**：序列开始标记
   - 在 ChatML 中：`<|im_start|>`
   - 用于分隔 user/assistant

3. **EOS (2)**：序列结束标记
   - 在 ChatML 中：`<|im_end|>`
   - 推理时遇到 EOS 停止生成

### 3.3 Tokenizer 加载

```python
from transformers import AutoTokenizer

# 加载自定义 tokenizer
tokenizer = AutoTokenizer.from_pretrained("../model/")

# 使用
text = "你好，世界"
tokens = tokenizer(text)
# {'input_ids': [123, 456, 789], 'attention_mask': [1, 1, 1]}

# 解码
decoded = tokenizer.decode([123, 456, 789])
# '你好，世界'
```

---

## 4. 常见数据问题

### 4.1 JSONL 格式错误排查

**问题**：`json.loads(line.strip())` 报错

**常见原因**：
1. JSON 格式不正确（缺少引号、括号）
2. 使用中文标点（应使用英文 `""` 而非 `""`）
3. 字段名错误（`"text"` 写成 `"txt"`）

**验证脚本**：
```python
import json

def validate_jsonl(file_path):
    with open(file_path, 'r', encoding='utf-8') as f:
        for line_num, line in enumerate(f, 1):
            try:
                data = json.loads(line.strip())
                # 检查必要字段
                if 'text' not in data and 'conversations' not in data:
                    print(f"Line {line_num}: 缺少必要字段")
            except json.JSONDecodeError as e:
                print(f"Line {line_num}: JSON 解析错误 - {e}")

validate_jsonl('data/pretrain_hq.jsonl')
```

### 4.2 长度截断问题

**问题**：长文本被截断后丢失信息

**解决方案**：

1. **分段处理**（推荐用于预训练）：
```python
def split_text(text, max_length=512, tokenizer):
    tokens = tokenizer.encode(text)
    chunks = []
    for i in range(0, len(tokens), max_length):
        chunk = tokens[i:i+max_length]
        chunks.append(tokenizer.decode(chunk))
    return chunks
```

2. **滑动窗口**（保留上下文）：
```python
def sliding_window(text, max_length=512, overlap=64, tokenizer):
    tokens = tokenizer.encode(text)
    chunks = []
    for i in range(0, len(tokens), max_length - overlap):
        chunk = tokens[i:i+max_length]
        chunks.append(tokenizer.decode(chunk))
    return chunks
```

3. **增加 max_length**（如果显存足够）：
```python
dataset = PretrainDataset(
    data_path,
    tokenizer,
    max_length=1024  # 从 512 增加到 1024
)
```

### 4.3 DDP 训练的数据分配

**问题**：DDP 多卡训练时数据分配不均

**解决方案**：使用 `DistributedSampler`

```python
from torch.utils.data import DistributedSampler

# 正确做法
train_sampler = DistributedSampler(train_ds)
train_loader = DataLoader(
    train_ds,
    batch_size=args.batch_size,
    sampler=train_sampler,  # 使用 sampler
    shuffle=False,           # sampler 已处理 shuffle
)
```

**工作原理**：
- 每个进程（GPU）获取数据的不同子集
- 确保每个样本只被一个 GPU 处理
- 每个 epoch 后自动 shuffle

---

## 5. 实战：准备自定义数据集

### 5.1 数据清洗 Checklist

```python
import json
import re

def clean_text(text):
    """文本清洗"""
    # 去除多余空格
    text = re.sub(r'\s+', ' ', text)
    # 去除特殊字符（保留中文、英文、标点）
    text = re.sub(r'[^\u4e00-\u9fa5a-zA-Z0-9，。！？、；：""''（）《》\s]', '', text)
    # 去除过短文本
    if len(text) < 10:
        return None
    return text

def validate_and_clean(input_file, output_file):
    """验证并清洗 JSONL 文件"""
    with open(input_file, 'r', encoding='utf-8') as f_in, \
         open(output_file, 'w', encoding='utf-8') as f_out:
        for line_num, line in enumerate(f_in, 1):
            try:
                data = json.loads(line.strip())
                # 清洗文本
                if 'text' in data:
                    cleaned = clean_text(data['text'])
                    if cleaned:
                        data['text'] = cleaned
                        f_out.write(json.dumps(data, ensure_ascii=False) + '\n')
                elif 'conversations' in data:
                    # 清洗对话
                    valid_conversations = []
                    for turn in data['conversations']:
                        cleaned = clean_text(turn['content'])
                        if cleaned:
                            turn['content'] = cleaned
                            valid_conversations.append(turn)
                    if len(valid_conversations) >= 2:  # 至少一轮对话
                        data['conversations'] = valid_conversations
                        f_out.write(json.dumps(data, ensure_ascii=False) + '\n')
            except Exception as e:
                print(f"Line {line_num}: {e}")

validate_and_clean('data/raw.jsonl', 'data/cleaned.jsonl')
```

### 5.2 格式转换脚本示例

**CSV → JSONL**：
```python
import pandas as pd
import json

def csv_to_jsonl(csv_file, jsonl_file, text_column='text'):
    """将 CSV 转换为 JSONL 预训练格式"""
    df = pd.read_csv(csv_file)
    with open(jsonl_file, 'w', encoding='utf-8') as f:
        for _, row in df.iterrows():
            data = {"text": row[text_column]}
            f.write(json.dumps(data, ensure_ascii=False) + '\n')

csv_to_jsonl('data/wikipedia.csv', 'data/pretrain_hq.jsonl')
```

**XML → JSONL (SFT 格式)**：
```python
import xml.etree.ElementTree as ET

def xml_to_sft_jsonl(xml_file, jsonl_file):
    """将 XML 对话数据转换为 SFT JSONL 格式"""
    tree = ET.parse(xml_file)
    root = tree.getroot()

    with open(jsonl_file, 'w', encoding='utf-8') as f:
        for conversation in root.findall('conversation'):
            turns = []
            for turn in conversation.findall('turn'):
                role = turn.get('role')
                content = turn.text
                turns.append({
                    "from": role,
                    "value": content
                })
            data = {"conversations": turns}
            f.write(json.dumps(data, ensure_ascii=False) + '\n')

xml_to_sft_jsonl('data/dialogues.xml', 'data/sft_data.jsonl')
```

### 5.3 数据验证方法

```python
def validate_dataset(file_path, dataset_type='pretrain'):
    """验证数据集"""
    from transformers import AutoTokenizer
    tokenizer = AutoTokenizer.from_pretrained('../model/')

    with open(file_path, 'r', encoding='utf-8') as f:
        for line_num, line in enumerate(f, 1):
            try:
                data = json.loads(line.strip())

                if dataset_type == 'pretrain':
                    # 验证预训练数据
                    assert 'text' in data, f"Line {line_num}: 缺少 'text' 字段"
                    text = data['text']
                    tokens = tokenizer.encode(text)
                    assert len(tokens) > 0, f"Line {line_num}: 空文本"

                elif dataset_type == 'sft':
                    # 验证 SFT 数据
                    assert 'conversations' in data, f"Line {line_num}: 缺少 'conversations' 字段"
                    convs = data['conversations']
                    assert len(convs) >= 2, f"Line {line_num}: 对话轮次少于 2"
                    assert len(convs) % 2 == 0, f"Line {line_num}: 对话轮次必须是偶数"

                elif dataset_type == 'dpo':
                    # 验证 DPO 数据
                    assert 'chosen' in data and 'rejected' in data, f"Line {line_num}: 缺少 'chosen' 或 'rejected' 字段"
                    assert len(data['chosen']) == len(data['rejected']), f"Line {line_num}: chosen 和 rejected 长度不一致"

            except Exception as e:
                print(f"验证失败: {e}")

    print(f"✅ {dataset_type.upper()} 数据验证通过")

validate_dataset('data/pretrain_hq.jsonl', 'pretrain')
validate_dataset('data/sft_mini_512.jsonl', 'sft')
validate_dataset('data/dpo_data.jsonl', 'dpo')
```

---

## 6. 数据质量分析

### 6.1 统计信息分析

```python
def analyze_dataset(file_path, dataset_type='pretrain'):
    """分析数据集统计信息"""
    from transformers import AutoTokenizer
    tokenizer = AutoTokenizer.from_pretrained('../model/')

    lengths = []
    total_samples = 0

    with open(file_path, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            total_samples += 1

            if dataset_type == 'pretrain':
                tokens = tokenizer.encode(data['text'])
                lengths.append(len(tokens))
            elif dataset_type == 'sft':
                prompt = tokenizer.apply_chat_template(
                    [{"role": 'user' if i % 2 == 0 else 'assistant', "content": t['content']}
                     for i, t in enumerate(data['conversations'])],
                    tokenize=False
                )
                tokens = tokenizer.encode(prompt)
                lengths.append(len(tokens))

    import numpy as np
    print(f"总样本数: {total_samples}")
    print(f"平均长度: {np.mean(lengths):.2f} tokens")
    print(f"中位数长度: {np.median(lengths):.2f} tokens")
    print(f"最大长度: {np.max(lengths)} tokens")
    print(f"最小长度: {np.min(lengths)} tokens")
    print(f"90分位数: {np.percentile(lengths, 90):.2f} tokens")

analyze_dataset('data/pretrain_hq.jsonl', 'pretrain')
```

### 6.2 可视化分布

```python
import matplotlib.pyplot as plt

def plot_length_distribution(file_path):
    """绘制长度分布图"""
    from transformers import AutoTokenizer
    tokenizer = AutoTokenizer.from_pretrained('../model/')

    lengths = []
    with open(file_path, 'r', encoding='utf-8') as f:
        for line in f:
            data = json.loads(line.strip())
            tokens = tokenizer.encode(data['text'])
            lengths.append(len(tokens))

    plt.figure(figsize=(10, 6))
    plt.hist(lengths, bins=50, edgecolor='black')
    plt.xlabel('Token Count')
    plt.ylabel('Frequency')
    plt.title('Dataset Length Distribution')
    plt.savefig('docs/length_distribution.png')
    print("分布图已保存到 docs/length_distribution.png")

plot_length_distribution('data/pretrain_hq.jsonl')
```

---

## 7. 总结

### 数据格式对比表

| 特性 | 预训练 | SFT | DPO |
|------|--------|-----|-----|
| **目标** | 学习语言知识 | 学习对话 | 学习偏好 |
| **格式** | 连续文本 | 多轮对话 | 对比对话 |
| **字段** | `text` | `conversations` | `chosen`, `rejected` |
| **Loss** | 全部计算 | 仅 assistant | 对比计算 |
| **max_length** | 512 | 1024 | 4096 |

### 核心要点

1. **PretrainDataset**：简单文本，计算全部位置的 loss
2. **SFTDataset**：对话格式，**动态损失掩码**是关键
3. **DPODataset**：对比学习，同时处理 chosen 和 rejected
4. **Tokenizer**：6400 小词表适合小模型
5. **数据质量**：清洗、验证、分析缺一不可

---

## 8. 延伸阅读

- **03-数据工程实战.md**：数据收集、清洗、增强的完整流程
- **05-训练流程完整指南.md**：数据如何输入模型进行训练

---

**文档版本**: v1.0
**最后更新**: 2026-02-17
